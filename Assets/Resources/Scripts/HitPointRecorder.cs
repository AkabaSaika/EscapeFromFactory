using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class HitPointRecorder : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Transform weapon = GameObject.Find("Sword Two-Hander Purple(Clone)").transform;
        
        
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
